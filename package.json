{
  "name": "bank-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && rm -rf node_modules && npm install --legacy-peer-deps && npm run build"
  },
  "engines": {
    "node": "20.x"
  }
}