{
  "name": "bank-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install && cd backend && npm install && cd ../frontend && npm install --legacy-peer-deps && npm run build"
  },
  "engines": {
    "node": "20.x"
  }
}